import { Routes } from '@angular/router';

import { OnboardingPageComponent } from './onboarding-page.component';

export const ONBOARDING_ROUTES: Routes = [{ path: '', component: OnboardingPageComponent }];
