/** Open Food Facts read-only API origin (AR-8). Used by future OffApiService and ngsw exclusion tests. */
export const OFF_API_ORIGIN = 'https://world.openfoodfacts.org';

export const OFF_API_URL_PATTERN = `${OFF_API_ORIGIN}/**`;
