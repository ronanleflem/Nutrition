/** Search-a-licious API (Phase 2 online search). Not cached by the service worker. */
export const OFF_SEARCH_ORIGIN = 'https://search.openfoodfacts.org';

export const OFF_SEARCH_URL_PATTERN = `${OFF_SEARCH_ORIGIN}/**`;
